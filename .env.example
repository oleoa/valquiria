# Resend — envio dos formulários (app/forms/*).
# Crie a chave em https://resend.com (Dashboard → API Keys) e cole o valor real em
# .env.local (que NÃO é versionado). Na Vercel, configure a mesma variável em
# Settings → Environment Variables (Production + Preview).
RESEND_API_KEY=
