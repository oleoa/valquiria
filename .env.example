# Strutura Email Gateway — envio dos formulários (app/forms/*). Ver EMAIL-GATEWAY.md.
# Peça a chave do projeto ao mantenedor do gateway (Leonardo) e cole o valor real em
# .env.local (que NÃO é versionado). Na Vercel, configure a mesma variável em
# Settings → Environment Variables (Production + Preview).
EMAIL_GATEWAY_KEY=
